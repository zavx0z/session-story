{
  "name": "session-story",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "bun run src/cli.ts prepare",
    "merge": "bun run src/cli.ts merge",
    "summarize": "bun run src/cli.ts summarize",
    "keyframes": "bun run src/cli.ts keyframes",
    "handoff": "bun run src/cli.ts handoff",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
